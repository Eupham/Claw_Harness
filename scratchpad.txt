The user reported an issue with "unauthorized: too many failed authentication attempts (retry later)" when attempting to connect to OpenClaw Gateway on Modal, and explicitly asked to "reset stuff and give me a notebook to launch from."

I will create the notebook as requested. OpenClaw rate limits failed authentication attempts. To resolve this cleanly for the user, we will bypass the token mismatch/auth attempts block by completely tearing down the existing config state and generating a new token and volume, and ensuring `gateway.auth.mode: "none"` is used temporarily for pairing or bypassing auth if it's safe to do so. Since it's a Modal web endpoint, we should probably stick to `"token"` but generate a fresh volume so it doesn't remember the failed attempts limit. We will just use a different Volume name.